// import { useSearchSpotifyMusic } from "./useSearchSpotifyMusic";
export {};

describe("useSearchSpotifyMusic", () => {
  it("should return music list", async () => {
    // NOTE: デバッグように書いたが、CIで実行できる状態になっていないので、コメントアウトしておく
    // expect(await useSearchSpotifyMusic("おもかげ Vaundy")).toBe([]);
  });
});
